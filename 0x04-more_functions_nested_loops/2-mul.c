#include "main.h"

/**
 * mul - returns results of multiplication of 2 integers
 * @a: represent first integer
 * @b: represent second integer
 * Return: result
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
		return(result);
}
