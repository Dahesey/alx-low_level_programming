#include "main.h"

/**
 * mul - returns result of multiplication between 2 integers
 *@a: first multiplier
 *@b: second multiplier
 *
 * Return: multiplication result
 */
int mul(a, b)
{
	int result;

	result = a * b;
	return(result);
}
