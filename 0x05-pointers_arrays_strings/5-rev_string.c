/**
 * rev_string - Reverses string
 *
 * Author - Dahesey
 *
 * @s: String to be reversed
 *
 * Return: void
 */

#include "main.h"

void rev_string(char *s)
{
	int x = 0;
	int y = 0;
	char *p;

	for (*p = s; s != '\0'; s++)
		x++;
	for (; j < i / 2; j++)
	{
		char keep;

		keep = *--s;
		*p = *s;
		*p++ = keep;
	}
}
