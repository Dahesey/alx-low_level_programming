#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: 0 (Success)
 */

int main(void);
{
	int ( x, y )
	x = 89;
	y = 7;
	mod = x % y
	{
		printf( "%d % %d is %d\n", x, y, mod);
	}

	return (0);

}
